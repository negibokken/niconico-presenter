{
  "name": "niconico-presenter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "electron": "^1.7.9",
    "foreman": "^2.0.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.0.17"
  },
  "main": "src/electron-starter.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "dev": "nf start -p 3000",
    "package":
      "electron-packager . hoge --platform=darwin --arc=x64 --out=release"
  },
  "devDependencies": {
    "electron-packager": "^10.1.0"
  }
}
